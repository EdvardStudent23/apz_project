[project]
name = "messaging"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = ["pydantic>=2", "aio-pika>=9"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/messaging"]
