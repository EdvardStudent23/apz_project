# Mongo quorum read-only

TODO: step-by-step procedure for proving the History service goes
read-only when the Mongo replica set loses majority (see `CLAUDE.md`
§ "Mongo quorum read-only"). Re-run before every release tag and
record the date + result here.
